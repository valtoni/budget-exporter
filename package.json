{
  "name": "budget-exporter",
  "version": "1.3.0",
  "description": "Detect bank pages and export to CSV.",
  "scripts": {
    "version:sync": "node scripts/sync-version.mjs",
    "prebuild": "node scripts/sync-version.mjs",
    "prebuild:watch": "node scripts/sync-version.mjs",
    "build": "node build.config.mjs",
    "build:watch": "node build.config.mjs --watch"
  },
  "devDependencies": {
    "@types/chrome": "^0.1.24",
    "esbuild": "^0.25.0"
  },
  "dependencies": {
    "@awesome.me/webawesome": "^3.7.0",
    "@fontsource-variable/source-sans-3": "^5.2.9",
    "flatpickr": "^4.6.13",
    "lit": "^3.2.1",
    "tom-select": "^2.4.3"
  }
}
